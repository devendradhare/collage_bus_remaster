import React from "react";
// import { useAuth0 } from "@auth0/auth0-react";

export default function Home() {
  // const { user, isAuthenticated, isLoading } = useAuth0();
  // return (
  //   <div>
  //     asdf
  //     {isAuthenticated?user:Home}
  //   </div>
  // )

  // if (isLoading) {
  //   return (
  //     <div>
  //       <div>Loading ...</div>
  //     </div>
  //   );
  // }

  return (
    // isAuthenticated &&
    <div>
      this is home
      {/* <img src={user.picture} alt={user.name} />
      <h2>
        {user.name}
      </h2>
      <p>
        {user.email}
      </p>
      <p>
        {user.nickname}
      </p> */}
    </div>
  );
}
